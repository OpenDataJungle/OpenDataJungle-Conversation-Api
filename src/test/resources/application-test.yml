# Configuration overrides applied on top of application.yml for the `test` profile.
spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

logging:
  level:
    com.opendatajungle: INFO
    org.springframework.ai: WARN
